{
  "name": "run-testproject",
  "version": "1.0.0",
  "private": true,
  "description": "Action for triggering a testproject job run",
  "main": "index.js",
  "scripts": {
    "prepare": "ncc build index.js -o dist --source-map",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vercel/ncc": "^0.28.6"
  },
  "dependencies": {
    "@actions/core": "^1.4.0",
    "axios": "^0.21.1"
  }
}
